rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // RTQ Cendekia Multi-Device Sync Collections
    match /rtq_sync/{document} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
